# settings.py

# Screen dimensions
WIDTH, HEIGHT = 1000, 500

# Colors
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
BLUE = (0, 128, 255)

# Dice settings
DICE_SIZE = 100
DICE_POSITIONS = [(100 + i * (DICE_SIZE + 20), 100) for i in range(6)]
